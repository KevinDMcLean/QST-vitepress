// .vitepress/config.mjs
import { defineConfig } from 'vitepress'

export default defineConfig({
  // Remove any fallback title
  title: '',

  themeConfig: {
    nav: [],
    logo: '' // no logo
  }
})
